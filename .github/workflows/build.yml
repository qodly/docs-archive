name: build

on:
  pull_request:
    branches: [main]
  pull_request_target:
    branches: [main]
  push: 
    branches:
      - '*/*'
      - '!main'

jobs:
  workflow-build:
    uses: ./.github/workflows/workflow-build.yml
